<main>
  <h2 class="createHeader">Create Your Own Quote!</h2>
  <a href="/">Return to Home Page</a>
  <div id="edit-form" class="quote-container-show">
    {{!-- Form to submit a new quote --}}
    <form action="/quote" method="POST">
      <div>
        <label for="content">content: </label>
        <input type="text" name="content">
      </div>
      <div>
        <label for="speaker">speaker: </label>
        <input type="text" name="speaker">
      </div>
      {{!-- Dropdown menu of available feelings to select from --}}
      <div class="form-group">
    <label for="name">Feeling:</label>
    <select name="feeling" class="custom-select" id="inputGroupSelect01">
    {{#each feeling}}
    <option value="{{_id}}">{{name}}</option>
    {{/each}}
  </select>
  </div>
</div>
      <button type="submit">Create New Quote</button>
    </form>
  </div>
</main>